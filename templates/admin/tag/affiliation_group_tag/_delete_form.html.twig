<form method="post" action="{{ path('admin_affiliation_group_tag_delete', {'id': affiliation_group_tag.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ affiliation_group_tag.id) }}">
    <button class="btn">Delete</button>
</form>
