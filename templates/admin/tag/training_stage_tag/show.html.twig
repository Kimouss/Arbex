{% extends 'admin/base.html.twig' %}

{% block title %}Parent Publication Tag{% endblock %}

{% block page_title %}Parent Publication Tag{% endblock %}
{% block page_subtitle %}Show{% endblock %}

{% block page_content %}
    <div class="box box-default">
        <div class="box-body">
            <table class="table">
                <tbody>
                <tr>
                    <th>{{ 'arbex.tag.title' | trans }}</th>
                    <td>{{ training_stage_tag.title }}</td>

                </tr>
                <tr>
                    <th>{{ 'arbex.publication.created_at' | trans }}</th>
                    <td>{{ training_stage_tag.createdAt | date('d-m-Y') }}</td>

                </tr>
                <tr>
                    <th>{{ 'arbex.publication.updated_at' | trans }}</th>
                    <td>{{ training_stage_tag.updatedAt | date('d-m-Y') }}</td>

                </tr>
                </tbody>
            </table>
        </div>

        <div class="box-footer">
            <div class="row">
                <div class="col-md-3">
                    <a href="{{ path('admin_training_stage_tag_index') }}" class="btn btn-default">back to list</a>
                    <a href="{{ path('admin_training_stage_tag_edit', {'id': training_stage_tag.id}) }}" class="btn btn-warning">edit</a>
                </div>

                {% if is_granted('ROLE_ADMIN_ARBEX') %}
                    <div class="col-md-2 pull-right">
                        {{ include('admin/tag/training_stage_tag/_delete_form.html.twig') }}
                    </div>
                {% endif %}
            </div>
        </div>
    </div>
{% endblock %}
