<form method="post" action="{{ path('admin_training_stage_tag_delete', {'id': training_stage_tag.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ training_stage_tag.id) }}">
    <button class="btn">Delete</button>
</form>
