<form method="post" action="{{ path('admin_parent_publication_tag_delete', {'id': parent_publication_tag.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ parent_publication_tag.id) }}">
    <button class="btn">Delete</button>
</form>
