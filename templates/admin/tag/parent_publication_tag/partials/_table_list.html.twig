<div class="box-body">
    <table class="table table-striped">
        <thead>
        <tr>
            <th scope="col">
                {{ knp_pagination_sortable(pagination, 'arbex.tag.title' | trans, 'parent_publication_tag.title') }}
                <i class="fas fa-{{ sortDirectionIcon('parent_publication_tag.title', pagination.params) }}"></i>
            </th>
            <th scope="col" class="text-center">
                {{ knp_pagination_sortable(pagination, 'arbex.tag.created_at' | trans, 'user.created_at') }}
                <i class="fas fa-{{ sortDirectionIcon('user.created_at', pagination.params) }}"></i>
            </th>
            <th scope="col" class="text-center">
                {{ knp_pagination_sortable(pagination, 'arbex.tag.updated_at' | trans, 'parent_publication_tag.updated_at') }}
                <i class="fas fa-{{ sortDirectionIcon('parent_publication_tag.updated_at', pagination.params) }}"></i>
            </th>
            <th scope="col" class="text-center">
                Action
            </th>
        </tr>
        </thead>
        <tbody>
        {% for parent_publication_tag in pagination %}
            <tr>
                <th scope="row">{{ parent_publication_tag.title }}</th>
                <td class="text-center">{{ parent_publication_tag.createdAt | date('d-m-Y') }}</td>
                <td class="text-center">{{ parent_publication_tag.updatedAt | date('d-m-Y') }}</td>
                <td class="text-center">
                    <div class="btn-group">
                        <a href="{{ path('admin_parent_publication_tag_show', {id: parent_publication_tag.id}) }}" class="btn-sm btn-success">
                            <i class="fas fa-eye"></i>
                        </a>
                    </div>
                    <div class="btn-group">
                        <a href="{{ path('admin_parent_publication_tag_edit', {id: parent_publication_tag.id}) }}" class="btn-sm btn-primary">
                            <i class="fas fa-edit"></i>
                        </a>
                    </div>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('admin_parent_publication_tag_new') }}" class="btn btn-primary">New</a>

    <div class="navigation">
        <div class="pull-right">
            {{ knp_pagination_render(pagination) }}
        </div>
    </div>
</div>
